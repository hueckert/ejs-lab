const express = require('express');
const app = express();

// server.js

// server.js

app.get('/', (req, res) => {
    res.render('home.ejs');
  });
  
  










app.get('/', (req, res) => {
  res.send('Hello There!');
});

app.listen(3000);
